html
  include blocks/head

  body
    include blocks/map_1.pug
    .container
      .head
        h1.head__title= title
        p.head__info= info
      .links
        each link in links
            a.links__link(class ='line' href=link.href)= link.name
